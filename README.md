# Greenpeace landing votacion bosques

## Install and build
1. `git clone https://github.com/dantovbein/greenpeace-landing-votacion-bosques.git app`.
2. `cd ./app`
3. `npm install`
4. `npm run build`.

The built content is into the `./out` directory.
